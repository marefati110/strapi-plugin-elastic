{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "elasticsearch.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/migrate",
      "handler": "elasticsearch.customMigrate",
      "config": {
        "policies": []
      }
    }
  ]
}
